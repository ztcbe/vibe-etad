services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: zvibe
      POSTGRES_PASSWORD: zvibe
      POSTGRES_DB: zvibe
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
